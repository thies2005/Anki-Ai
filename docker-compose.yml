services:
  anki-ai:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONIOENCODING=UTF-8
    # Interactive mode for debugging
    stdin_open: true
    tty: true
